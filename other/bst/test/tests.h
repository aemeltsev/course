#ifndef BST_TESTS_H
#define BST_TESTS_H

#include <iostream>
#include <ctime>
#include <gtest/gtest.h>
#include "bst.h"
/*
#include <vector>
#include "bst.h"

bst::BST<int> make_bst()
{
    auto tree = bst::BST<int>{4};
    tree.insert(5);
    tree.insert(2);
    tree.insert(1);
    tree.insert(3);
    return tree;
}

void test_bundle()
{
    auto a_tree = make_bst();
    auto b_tree = make_bst();
    auto c_tree = make_bst();
    auto in_vect = a_tree.get_in_order();
    auto pre_vect = b_tree.get_pre_order();
    auto pos_vect = c_tree.get_post_order();
}

int main(int argc, char *argv[])
{
    test_bundle();
}
*/

#endif //BST_TESTS_H
